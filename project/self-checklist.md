# Самопроверка проекта (self-checklist)

Отметьте, что реализовано в вашем проекте.  
Этот чеклист помогает и вам, и преподавателю быстро понять, на каком уровне готовности находится проект.

В столбце «Да/Нет (студент)» поставьте `✅` или `❌`.  
В столбце «Где смотреть / комментарий» укажите файл(ы), где реализован соответствующий пункт.

> Важно: чеклист – это **самооценка**. Преподаватель может не согласиться с вашими отметками и скорректировать оценку.

---

## Таблица самопроверки

| #  | Критерий                                                                 | Да/Нет (студент) | Где смотреть / комментарий                          |
|----|---------------------------------------------------------------------------|------------------|-----------------------------------------------------|
| 1  | Сервис запускается по инструкциям из `project/README.md` и работает      | ✅ | `README.md` раздел «Как запустить». Команда `uv run uvicorn src.service.app:app --host 0.0.0.0 --port 8000`; Docker: `docker build -t tour-recommender . && docker run -p 8000:8000 tour-recommender` |
| 2  | Endpoint `/predict` использует **реальную модель**, а не заглушку        | ✅ | `src/service/app.py` → `predict()` вызывает `get_similar_tours()` из `src/models/recommender.py`; матрица схожести `artifacts/exp03/sim_combined.npy` загружается при старте |
| 3  | Есть EDA и хотя бы один эксперимент с метриками                          | ✅ | `notebooks/exp01_eda.ipynb` — полный EDA (распределения, корреляции, сезонность, групповая аналитика); `notebooks/exp02_baseline.ipynb` — эксперимент с пятью метриками; `artifacts/exp02/metrics_comparison.csv` |
| 4  | Есть baseline и улучшенная модель, есть **сравнение по метрикам**        | ✅ | Baseline — популярность в городе (`src/models/recommender.py:get_popularity_recs`); улучшенные — struct, TF-IDF, combined; сравнение в `notebooks/exp02_baseline.ipynb` и `artifacts/exp02/metrics_comparison.csv`; подбор весов в `notebooks/exp03_weight_tuning.ipynb` |
| 5  | Код не свален в один ноутбук: есть внятная структура в `src/`            | ✅ | `src/models/features.py` — построение матриц признаков; `src/models/recommender.py` — логика рекомендаций; `src/models/metrics.py` — все метрики качества; `src/service/app.py` — FastAPI-сервис |
| 6  | Есть Dockerfile **или** понятный сценарий развёртывания без Docker       | ✅ | `Dockerfile` (python:3.12-slim, COPY src/ + artifacts/exp03/, EXPOSE 8000); инструкции в `README.md` раздел «Запуск сервиса» |
| 7  | Есть `.env.example` и **нет** в репозитории реальных секретов/паролей    | ✅ | `configs/.env.example` — документирует переменную `CONFIG_PATH` (путь к конфигу, имеет безопасное значение по умолчанию). Реальных секретов в репозитории нет: данные синтетические, внешние API не используются |
| 8  | Реализованы логи/наблюдаемость (хотя бы консольные логи + `/health`)     | ✅ | `src/service/app.py` — `logging` Python (уровень из `config.yaml`); логируются startup, каждый `/predict` (tour_id, top_k, elapsed_ms), ошибки; endpoint `GET /health` возвращает `{"status": "ok", "tours_loaded": 170, "sim_matrix_shape": [170, 170]}` |
| 9  | В `report.md` **обоснован выбор финальной модели** по результатам экспериментов | ✅ | `report.md` разделы 5.2–5.4: таблица сравнения 4 моделей, grid search по весу (exp03), обоснование `w_struct=0.7` как баланса CHR@5=0.936 и Coverage=1.000 |
| 10 | `project/README.md` и `report.md` позволяют понять сценарий демонстрации | ✅ | `README.md` раздел «Демонстрация на защите» — команды запуска, curl-примеры; `report.md` раздел 9 — детальный сценарий с двумя ключевыми демонстрациями |

---

## Подсчёт баллов и связь с оценкой

Итоговая самооценка: **10 / 10** баллов.

За каждый критерий, по которому вы честно можете поставить `✅`, засчитывается **1 балл**.  
Максимум – 10 баллов.

Преподаватель использует суммарное количество баллов **после проверки** в качестве ориентира для выставления оценки:

- Если проект **не проходит базовый минимум** (не запускается, отсутствует ключевой функционал, грубые нарушения, плагиат и т.п.) – оценка: **2** (проходной минимум не достигнут).
- Если минимум пройден, то ориентировочно:
  - **1-4 балла** → оценка **3** (проект принят, но на базовом уровне);
  - **5-8 баллов** → оценка **4** (хороший, рабочий проект);
  - **9-10 баллов** → оценка **5** (сильный, хорошо проработанный проект).

Окончательное решение по оценке остаётся за преподавателем и может учитывать:

- качество реализации внутри каждого пункта;
- дополнительные сильные стороны проекта (нестандартные решения, дополнительные функции, аккуратная архитектура и т.п.);
- соблюдение требований курса и дедлайнов.

---
